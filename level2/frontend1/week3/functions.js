export function sum( num1, num2 ) {
  alert(`The sumatory result is ${num1 + num2}`);
}
export function rest(num1,num2) {
  alert(`The rest result is ${num1 - num2}`);
}
export function mult(num1,num2) {
  alert(`The multiplication result is ${num1 * num2} `);
}

export function div(num1,num2) {
  if(b === 0){alert(`Forbiden operation, try without dividing by the number 0 again!`);}
  alert(`The division result is ${num1 / num2}`);
  return;
}
