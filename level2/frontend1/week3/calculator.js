import  from="./functions.js";

const EXIT = 'pass_key';

let word;
let counter = 0;
let num1, num2;

do{
	if(option > 0 && < 5){
		num1 = parseInt(prompt(`Insert the first number to:`));
		num2 = parseInt(prompt(`Insert the second number to:`));
	}
	switch (option) {

		case 1:

			break;
		case 2:

			break;
		case 3:
			break;
		case 4:
			break;
		case 5:
			break;
		default:
			break;
	}




	counter ++;

}while(!(word===EXIT));
